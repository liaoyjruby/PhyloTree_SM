##### Setup #####
configfile: "config/config.yaml"
workdir: config['dir']

##### Load Rules #####
include: "rules/ref.smk"
include: "rules/validation.smk"
include: "rules/persample.smk"
include: "rules/mergesamples.smk"

##### Target Rules #####
rule all:
    input:
        expand("references/{ref}", ref=config["references"].values())

